# LensView Nginx 安全配置验证报告

**测试时间：** 2026-04-20 10:30 GMT+8  
**测试环境：** OpenCloudOS + Nginx 1.26.3

---

## ✅ 验证通过的安全配置

### 1. 安全响应头测试
| 响应头 | 状态 | 值 |
|--------|------|-----|
| X-Frame-Options | ✅ | SAMEORIGIN |
| X-Content-Type-Options | ✅ | nosniff |
| X-XSS-Protection | ✅ | 1; mode=block |
| Referrer-Policy | ✅ | strict-origin-when-cross-origin |
| Content-Security-Policy | ✅ | 已配置 |

**测试命令：**
```bash
curl -sI http://localhost | grep -E "^(X-|Content-Security|Referrer)"
```

---

### 2. 敏感文件访问保护
| 文件 | HTTP 状态码 | 结果 |
|------|------------|------|
| /.env | 404 | ✅ 已阻止 |
| /.git/config | 404 | ✅ 已阻止 |
| /.bashrc | 404 | ✅ 已阻止 |

**测试命令：**
```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost/.env
curl -s -o /dev/null -w "%{http_code}" http://localhost/.git/config
```

---

### 3. Nginx 版本隐藏
| 项目 | 结果 |
|------|------|
| Server 头 | `nginx`（无版本号）✅ |

**测试命令：**
```bash
curl -sI http://localhost | grep "Server:"
```

---

### 4. 限流配置测试
| 测试场景 | 结果 |
|----------|------|
| 前 20 个请求 | 200 OK ✅ |
| 第 21-30 个请求 | 503 Service Unavailable ✅ |

**说明：** burst=20 允许瞬间 20 个请求，超出后返回 503

**测试命令：**
```bash
for i in {1..30}; do curl -s -o /dev/null -w "%{http_code} " http://localhost/; done
```

---

### 5. 请求大小限制
| 配置项 | 值 |
|--------|-----|
| client_max_body_size | 25M |
| 适用场景 | 支持 20MB 图片上传 + 余量 |

**测试命令（上传大文件）：**
```bash
# 创建一个 30MB 的文件测试
dd if=/dev/zero of=/tmp/test30m.bin bs=1M count=30
curl -X POST http://localhost/api/upload \
  -F "photo=@/tmp/test30m.bin" \
  -F "category=测试"
# 应该返回 413 Request Entity Too Large
```

---

## 📊 配置摘要

**配置文件位置：**
- 主配置：`/etc/nginx/nginx.conf`
- 站点配置：`/etc/nginx/conf.d/lensview.conf`

**关键配置：**
```nginx
# 限流 Zone（在 http 块中）
limit_req_zone $binary_remote_addr zone=lensview_limit:10m rate=10r/s;
limit_conn_zone $binary_remote_addr zone=lensview_conn:10m;

# 隐藏版本
server_tokens off;

# 请求大小限制
client_max_body_size 25M;

# 安全头
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-Content-Type-Options "nosniff" always;
add_header X-XSS-Protection "1; mode=block" always;
```

---

## 🧪 快速验证脚本

保存为 `verify-security.sh`，随时运行：

```bash
#!/bin/bash
echo "=== LensView 安全配置验证 ==="
echo ""

echo "1. 安全响应头:"
curl -sI http://localhost | grep -E "^(X-|Content-Security|Referrer|Server)"
echo ""

echo "2. 敏感文件访问（应全为 404）:"
echo -n "  .env: "; curl -s -o /dev/null -w "%{http_code}\n" http://localhost/.env
echo -n "  .git: "; curl -s -o /dev/null -w "%{http_code}\n" http://localhost/.git/config
echo ""

echo "3. 限流测试（30 个请求，应该有 503）:"
for i in {1..30}; do curl -s -o /dev/null -w "%{http_code} " http://localhost/; done
echo ""
echo ""

echo "✅ 验证完成！"
```

---

## ⚠️ 注意事项

1. **限流可能影响正常用户** - 如果并发用户多，可调整 `rate` 和 `burst`
2. **X-Powered-By 头** - 后端 Express 仍暴露，可在 server.js 中禁用：
   ```javascript
   app.disable('x-powered-by');
   ```
3. **HTTPS 未配置** - 生产环境建议启用 HTTPS

---

## 📝 后续建议

- [ ] 配置 HTTPS（Let's Encrypt 免费证书）
- [ ] 配置防火墙（只开放 80/443）
- [ ] 设置日志监控和告警
- [ ] 定期备份配置和数据
