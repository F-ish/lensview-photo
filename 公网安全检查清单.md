# LensView 公网暴露安全检查清单

## ✅ 已完成的安全配置

### 1. 限流防护
- [x] 普通请求：5 请求/秒
- [x] 登录接口：3 请求/分钟（防密码爆破）
- [x] 并发连接：5 个/IP

### 2. Nginx 安全配置
- [x] 隐藏版本号
- [x] 安全响应头（XSS、点击劫持防护）
- [x] 敏感文件访问阻止
- [x] 请求大小限制（25MB）

### 3. 后端安全
- [x] bcrypt 密码哈希
- [x] JWT Token 认证（7 天过期）
- [x] 权限控制（未登录不能上传）
- [x] 文件类型二次验证

---

## ⚠️ 你必须做的事

### 立即执行（高危）

#### 1. 修改默认密码
```bash
cd /root/.openclaw/workspace/lensview
node init-admin.js
```
**密码要求：**
- 至少 12 位
- 包含大小写字母、数字、符号
- 不要用常见单词

#### 2. 设置 .env 文件权限
```bash
chmod 600 /root/.openclaw/workspace/lensview/.env
chown root:root /root/.openclaw/workspace/lensview/.env
ls -la /root/.openclaw/workspace/lensview/.env
```

#### 3. 获取你的公网 IP
```bash
# 方法 1
curl ifconfig.me

# 方法 2
curl cip.cc

# 方法 3：百度搜索"IP"
```

---

### 强烈推荐（中危）

#### 4. 防火墙限制 IP（如果只有你访问）
```bash
# 你的 IP（替换下面的 YOUR_IP）
YOUR_IP="123.45.67.89"

# 删除公开访问
sudo firewall-cmd --permanent --remove-service=http

# 只允许你的 IP
sudo firewall-cmd --permanent --add-rich-rule="
  rule family=\"ipv4\" 
  source address=\"$YOUR_IP\" 
  port port=\"80\" protocol=\"tcp\" accept"

sudo firewall-cmd --reload

# 验证
sudo firewall-cmd --list-all
```

#### 5. 配置日志监控
```bash
# 查看登录尝试
sudo tail -f /var/log/nginx/lensview_access.log | grep "/api/login"

# 查看失败登录
sudo tail -f /var/log/nginx/lensview_error.log
```

---

### 建议执行（低危）

#### 6. 定期备份
```bash
# 创建备份脚本
cat > /root/.openclaw/workspace/lensview/backup.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/backup/lensview/$(date +%Y%m%d_%H%M%S)"
mkdir -p $BACKUP_DIR
cp -r /root/.openclaw/workspace/lensview/photos $BACKUP_DIR/
cp /root/.openclaw/workspace/lensview/.env $BACKUP_DIR/
echo "备份完成：$BACKUP_DIR"
EOF

chmod +x /root/.openclaw/workspace/lensview/backup.sh

# 手动测试
./backup.sh
```

#### 7. 设置失败登录告警
```bash
# 监控登录失败（添加到 crontab）
*/5 * * * * grep "401" /var/log/nginx/lensview_access.log | \
  grep "/api/login" | \
  tail -10 >> /var/log/lensview_failed_logins.log
```

---

## 🧪 安全测试

### 测试 1：限流是否生效
```bash
# 快速发送 20 个请求
for i in {1..20}; do 
  curl -s -o /dev/null -w "%{http_code} " http://你的IP/
done
echo ""
# 应该看到部分 503
```

### 测试 2：登录限流
```bash
# 快速尝试登录 10 次
for i in {1..10}; do 
  curl -s -o /dev/null -w "%{http_code} " \
    -X POST http://你的IP/api/login \
    -H "Content-Type: application/json" \
    -d '{"username":"admin","password":"wrong"}'
done
echo ""
# 应该看到部分 429 或 503
```

### 测试 3：敏感文件访问
```bash
# 应该全部返回 404
curl -o /dev/null -w "%{http_code}\n" http://你的IP/.env
curl -o /dev/null -w "%{http_code}\n" http://你的IP/.git/config
curl -o /dev/null -w "%{http_code}\n" http://你的IP/.bashrc
```

### 测试 4：未授权访问
```bash
# 应该返回 401
curl -o /dev/null -w "%{http_code}\n" \
  -X POST http://你的IP/api/upload \
  -F "photo=@test.jpg"
```

---

## 📊 当前安全等级评估

| 配置项 | 状态 | 风险等级 |
|--------|------|---------|
| 强密码 | ⏳ 待设置 | 🔴 如果不设置 |
| 限流防护 | ✅ 已配置 | 🟢 |
| 防火墙 | ⏳ 可选 | 🟡 建议配置 |
| HTTPS | ❌ 未配置 | 🔴（HTTP 明文） |
| 日志监控 | ⏳ 可选 | 🟡 |
| 备份 | ⏳ 可选 | 🟡 |

**整体评估：** 🟡 中等风险（启用 HTTPS 前）

---

## 🚨 危险信号（需要立即处理）

如果看到以下情况：

1. **大量 401 错误** - 可能被密码爆破
   ```bash
   sudo grep "401" /var/log/nginx/lensview_access.log | wc -l
   ```

2. **大量 429/503 错误** - 可能被 DDoS
   ```bash
   sudo grep "503" /var/log/nginx/lensview_access.log | wc -l
   ```

3. **异常 IP 访问** - 陌生 IP 频繁访问
   ```bash
   sudo awk '{print $1}' /var/log/nginx/lensview_access.log | sort | uniq -c | sort -rn | head -20
   ```

**应对措施：**
- 立即修改密码
- 配置防火墙限制 IP
- 考虑暂时关闭服务

---

## 📞 快速检查命令

```bash
# 1. 服务状态
systemctl status nginx
systemctl status lensview

# 2. 端口监听
ss -tlnp | grep :80

# 3. 最近登录尝试
sudo tail -50 /var/log/nginx/lensview_access.log | grep "/api/login"

# 4. 当前连接数
sudo netstat -an | grep :80 | wc -l
```

---

## ✅ 公网访问检查清单

- [ ] 修改了强密码（不是 admin123）
- [ ] .env 文件权限设置为 600
- [ ] 获取了公网 IP 地址
- [ ] 测试了本地访问正常
- [ ] 配置了防火墙（推荐限制 IP）
- [ ] 设置了日志监控
- [ ] 创建了备份脚本
- [ ] 进行了安全测试

**全部完成后才能在公网暴露！**
