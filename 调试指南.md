# 照片查看功能调试指南

## 🧪 测试步骤

### 1. 访问测试页面
```
http://localhost/test-view.html
```
点击灰色方块，应该会弹出提示框。

**如果测试页面正常：** 说明浏览器 JavaScript 执行正常
**如果测试页面也不正常：** 说明浏览器有问题

---

### 2. 检查浏览器控制台

按 `F12` 打开开发者工具，查看 Console 标签：

**可能出现的错误：**

#### 错误 1: `viewPhoto is not defined`
**原因：** JavaScript 未正确加载
**解决：** 
- 强制刷新 `Ctrl+Shift+R`
- 清除浏览器缓存

#### 错误 2: `Cannot read property 'classList' of null`
**原因：** 模态框元素未找到
**解决：** 检查 HTML 是否正确加载

#### 错误 3: 无错误但点击无反应
**原因：** 点击事件未绑定
**解决：** 在 Console 手动执行测试

---

### 3. 手动测试（在 Console 中执行）

```javascript
// 测试 1：检查模态框是否存在
document.getElementById('photoModal')
// 应该返回：<div id="photoModal" ...>

// 测试 2：检查 viewPhoto 函数是否存在
viewPhoto
// 应该返回：ƒ viewPhoto(url, filename, category) {...}

// 测试 3：手动触发查看
viewPhoto('/images/uncategorized/img_xxx.jpg', '测试', 'uncategorized')
// 应该弹出模态框
```

---

### 4. 检查网络请求

在 Network 标签中：
- 刷新页面
- 检查 `index.html` 是否加载最新版本
- 查看是否有 404 或其他错误

---

## 🔍 常见问题解决

### 问题 1：浏览器缓存
**症状：** 代码已更新，但页面还是旧的

**解决方法：**
```
Chrome/Edge: Ctrl + Shift + R
Firefox: Ctrl + F5
Mac: Cmd + Shift + R
```

或者：
1. 按 F12
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

---

### 问题 2：点击事件被阻止
**症状：** 点击照片无反应

**检查：**
```javascript
// 在 Console 执行
document.querySelectorAll('.hover-scale')[0]
// 然后点击返回的元素，看是否有 onclick 属性
```

**解决：** 确保没有 CSS `pointer-events: none`

---

### 问题 3：模态框被遮挡
**症状：** 点击后模态框出现了，但看不到

**检查：**
```javascript
document.getElementById('photoModal').style.zIndex
// 应该返回 "9999" 或更大的值
```

**解决：** 修改 z-index 为更大的值

---

## 📝 验证清单

- [ ] 测试页面正常工作
- [ ] Console 无 JavaScript 错误
- [ ] 模态框 HTML 存在于页面中
- [ ] `viewPhoto` 函数已定义
- [ ] 照片卡片有 `cursor-pointer` 样式
- [ ] 点击照片触发 `viewPhoto` 函数

---

## 🎯 快速诊断命令

在浏览器 Console 中执行：

```javascript
// 1. 检查模态框
console.log('模态框:', document.getElementById('photoModal'));

// 2. 检查函数
console.log('viewPhoto 函数:', typeof viewPhoto);

// 3. 检查照片卡片
console.log('照片卡片数量:', document.querySelectorAll('.hover-scale').length);

// 4. 测试查看功能
viewPhoto('https://via.placeholder.com/800x600', '测试', '测试分类');
```

---

## 📞 如果还是不行

请提供以下信息：

1. **浏览器控制台截图**（F12 → Console）
2. **Network 标签截图**（F12 → Network）
3. **执行以下命令的结果：**
   ```javascript
   console.log('模态框:', document.getElementById('photoModal'));
   console.log('函数类型:', typeof viewPhoto);
   ```
