# 🔧 照片预览无法工作的解决方案

## ⚠️ 最可能的原因：浏览器缓存

你的浏览器正在使用旧版本的页面代码！

---

## ✅ 解决方法（按顺序尝试）

### 方法 1：强制刷新（推荐）

**Windows/Linux:**
```
Ctrl + Shift + R
```

**Mac:**
```
Cmd + Shift + R
```

---

### 方法 2：清除缓存

**Chrome/Edge:**
1. 按 `Ctrl + Shift + Delete`
2. 选择"缓存的图片和文件"
3. 时间范围选择"全部时间"
4. 点击"清除数据"

**Firefox:**
1. 按 `Ctrl + Shift + Delete`
2. 勾选"缓存"
3. 点击"立即清除"

---

### 方法 3：开发者工具禁用缓存

1. 按 `F12` 打开开发者工具
2. 按 `F1` 打开设置
3. 找到"Disable cache (while DevTools is open)"
4. 勾选这个选项
5. 刷新页面

---

### 方法 4：无痕模式测试

**Chrome/Edge:** `Ctrl + Shift + N`  
**Firefox:** `Ctrl + Shift + P`

在无痕窗口中访问 `http://localhost/`

---

## 🧪 验证方法

### 步骤 1：访问测试页面
```
http://localhost/test-photo-view.html
```

点击照片，**如果能正常预览**，说明功能正常，是主页缓存问题。

### 步骤 2：在主页 Console 中执行

按 `F12` 打开 Console，输入：

```javascript
console.log('viewPhoto 函数:', typeof viewPhoto);
console.log('currentPhotos:', currentPhotos);
console.log('photoModal:', document.getElementById('photoModal'));
```

**期望输出：**
```
viewPhoto 函数: function
currentPhotos: Array(1) [...]
photoModal: <div id="photoModal" ...>
```

如果显示 `undefined` 或 `null`，说明代码未正确加载 → **清除缓存**

---

### 步骤 3：手动测试

在 Console 中执行：

```javascript
currentPhotos = [{url: 'https://via.placeholder.com/800x600', filename: '测试', category: '测试'}];
viewPhoto(0);
```

如果弹出预览窗口，说明功能正常 → **是点击事件未绑定**

---

## 🔍 如果还是不行

### 检查 1：查看 Console 错误

按 `F12` → Console，看是否有红色错误信息

**常见错误：**
- `viewPhoto is not defined` → 代码未加载，清除缓存
- `Cannot read property 'classList' of null` → 模态框未找到，HTML 未加载
- `currentPhotos is not defined` → 变量未定义，代码顺序问题

### 检查 2：查看 Network

按 `F12` → Network，刷新页面

- 查看 `index.html` 是否加载成功（状态码 200）
- 查看是否有 JS 文件加载失败

### 检查 3：检查元素

按 `F12` → Elements，查找：

```html
<div id="photoModal" ...>
```

如果找不到，说明 HTML 未正确插入

---

## 🎯 最终解决方案

如果以上方法都无效，运行以下命令重新部署：

```bash
# 1. 重启服务
sudo systemctl restart lensview

# 2. 清除 Nginx 缓存（如果有）
sudo rm -rf /var/cache/nginx/*

# 3. 重启 Nginx
sudo systemctl restart nginx

# 4. 验证
curl http://localhost/ | grep "viewPhoto"
```

---

## 📞 测试结果

请测试后告诉我：

1. **测试页面** (`/test-photo-view.html`) 能否正常预览？
2. **Console** 中有什么错误信息？
3. 执行 `typeof viewPhoto` 返回什么？

这样我可以更准确地诊断问题！
