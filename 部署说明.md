# LensView 安全加固版 - 部署说明

## 🔐 安全更新摘要

### 已修复的安全隐患
1. ✅ **文件上传验证** - 使用 `file` 命令二次验证真实文件类型
2. ✅ **路径穿越防护** - 清理分类名，禁止特殊字符
3. ✅ **安全文件名** - 使用 `crypto.randomBytes` 生成随机文件名
4. ✅ **文件权限控制** - 上传文件权限设置为 0o640
5. ✅ **CORS 限制** - 只允许指定域名访问
6. ✅ **请求大小限制** - JSON 限制 1MB，文件限制 20MB
7. ✅ **敏感文件保护** - 禁止访问 `.env`, `.git` 等文件
8. ✅ **Nginx 安全头** - XSS、点击劫持防护

### 新增功能
1. ✅ **管理员登录** - 使用 bcrypt 哈希存储密码
2. ✅ **JWT 认证** - 7 天有效期 Token
3. ✅ **权限控制** - 只有管理员能上传/删除图片
4. ✅ **公开浏览** - 未登录用户可查看图片

---

## 🚀 快速部署

### 1. 安装依赖
```bash
cd /root/.openclaw/workspace/lensview
npm install
```

### 2. 初始化管理员账号
```bash
# 自动初始化（默认密码：admin123）
node init-admin-auto.js

# 或手动设置密码
node init-admin.js
```

### 3. 启动服务
```bash
# 开发模式
npm run dev

# 生产模式
npm start

# 后台运行
nohup npm start > lensview.log 2>&1 &
```

### 4. 访问网站
打开浏览器访问：http://localhost

---

## 👤 默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | admin123 |

**⚠️ 首次登录后请尽快修改密码！**

修改方法：
1. 编辑 `.env` 文件
2. 运行 `node init-admin.js` 重新生成密码哈希
3. 重启服务

---

## 🔧 配置说明

### 环境变量（.env）
```bash
# 管理员账号
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=<bcrypt 哈希>

# JWT 密钥（请保持随机）
JWT_SECRET=<随机字符串>

# 服务配置
PORT=3000
HOST=127.0.0.1

# CORS 允许的来源
ALLOWED_ORIGINS=http://localhost,http://127.0.0.1
```

### Nginx 配置
已配置安全加固，位置：`/etc/nginx/conf.d/lensview.conf`

主要功能：
- 限流：10 请求/秒
- 安全响应头
- 敏感文件访问阻止
- 反向代理到 3000 端口

---

## 📡 API 接口

### 认证接口

#### 登录
```bash
POST /api/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}

# 响应
{
  "success": true,
  "data": {
    "token": "eyJhbGc...",
    "user": { "username": "admin", "role": "admin" },
    "expiresIn": 604800
  }
}
```

#### 验证 Token
```bash
GET /api/auth/verify
Authorization: Bearer <token>

# 响应
{
  "valid": true,
  "user": { "username": "admin", "role": "admin" }
}
```

#### 登出
```bash
POST /api/logout
# 客户端删除 localStorage 中的 token 即可
```

### 图片接口

#### 上传图片（需要管理员权限）
```bash
POST /api/upload
Authorization: Bearer <token>
Content-Type: multipart/form-data

表单字段：
- photo: 图片文件（必填）
- category: 分类（必填）
- title: 标题（可选）
- description: 描述（可选）
```

#### 获取分类列表
```bash
GET /api/categories
# 公开访问

# 响应
{
  "categories": ["人像", "风景", "城市", ...]
}
```

#### 获取某分类下的图片
```bash
GET /api/images/:category
# 公开访问

# 响应
{
  "images": [
    {
      "filename": "img_xxx.jpg",
      "url": "/images/风景/img_xxx.jpg"
    }
  ]
}
```

#### 删除图片（需要管理员权限）
```bash
DELETE /api/images/:category/:filename
Authorization: Bearer <token>
```

---

## 🔒 权限说明

| 操作 | 未登录用户 | 管理员 |
|------|-----------|--------|
| 浏览图片 | ✅ | ✅ |
| 查看分类 | ✅ | ✅ |
| 上传图片 | ❌ | ✅ |
| 删除图片 | ❌ | ✅ |
| 查看完整路径 | ❌ | ✅ |

---

## 🧪 安全测试

### 1. 测试未授权访问
```bash
# 应该返回 401
curl -X POST http://localhost/api/upload \
  -F "photo=@test.jpg" \
  -F "category=风景"
```

### 2. 测试登录
```bash
# 获取 Token
TOKEN=$(curl -s -X POST http://localhost/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' \
  | jq -r '.data.token')

# 使用 Token 上传
curl -X POST http://localhost/api/upload \
  -H "Authorization: Bearer $TOKEN" \
  -F "photo=@test.jpg" \
  -F "category=风景"
```

### 3. 测试敏感文件访问
```bash
# 应该返回 404
curl http://localhost/.env
curl http://localhost/.git/config
```

### 4. 测试限流
```bash
# 快速发送 30 个请求
for i in {1..30}; do 
  curl -s -o /dev/null -w "%{http_code} " http://localhost/
done
# 应该看到部分 503
```

---

## 📝 文件结构

```
lensview/
├── server.js              # 主服务器（安全加固版）
├── index.html             # 前端页面（带登录功能）
├── package.json           # 依赖配置
├── .env                   # 环境变量（不要提交！）
├── .env.example           # 环境变量模板
├── .gitignore             # Git 忽略配置
├── init-admin.js          # 手动初始化脚本
├── init-admin-auto.js     # 自动初始化脚本
├── middleware/
│   └── auth.js            # 认证中间件
├── photos/                # 图片存储目录
│   ├── 人像/
│   ├── 风景/
│   └── ...
└── nginx-secure.conf      # Nginx 安全配置
```

---

## ⚠️ 注意事项

1. **保护 .env 文件** - 包含密码哈希和 JWT 密钥，不要提交到 Git
2. **定期备份** - 备份 `photos/` 目录和 `.env` 文件
3. **修改默认密码** - 首次登录后立即修改
4. **HTTPS** - 生产环境建议启用 HTTPS
5. **日志监控** - 定期检查 `lensview.log` 和 Nginx 日志

---

## 🔧 故障排查

### 服务无法启动
```bash
# 查看日志
tail -f lensview.log

# 检查端口占用
lsof -i :3000

# 检查 .env 配置
cat .env
```

### 登录失败
```bash
# 检查密码哈希是否正确
cat .env | grep ADMIN_PASSWORD_HASH

# 重新初始化密码
node init-admin-auto.js
```

### 上传失败
```bash
# 检查照片目录权限
ls -la photos/

# 检查文件大小限制
# Nginx: client_max_body_size 25M
# 后端：MAX_FILE_SIZE = 20MB
```

---

## 📞 技术支持

如有问题，请查看：
- 应用日志：`lensview.log`
- Nginx 日志：`/var/log/nginx/lensview_error.log`
- 安全配置文档：`SECURITY.md`
