# LensView 照片预览功能说明

## ✨ 新功能

### 照片大图预览
- ✅ 点击任意照片卡片，弹出大图预览
- ✅ 点击背景关闭预览
- ✅ 支持上一张/下一张切换
- ✅ 支持键盘快捷键操作

---

## 🎮 使用方法

### 方法 1：鼠标操作
1. **打开预览** - 点击任意照片卡片
2. **切换照片** - 点击左右箭头按钮（悬停时显示）
3. **关闭预览** - 点击背景或右上角 × 按钮

### 方法 2：键盘快捷键
- **← 左箭头** - 上一张照片
- **→ 右箭头** - 下一张照片
- **Esc** - 关闭预览

---

## 🔧 功能特性

### 1. 智能预加载
查看当前照片时，自动预加载下一张，切换更流畅

### 2. 照片计数
显示当前位置：`3 / 20`（第 3 张，共 20 张）

### 3. 分类信息
显示照片所属分类

### 4. 响应式设计
- 最大宽度：6xl（约 1280px）
- 最大高度：85vh（屏幕高度的 85%）
- 图片自适应，保持原始比例

---

## 🎨 UI 设计

### 预览窗口
- **背景** - 深色半透明（95% 不透明度）
- **层级** - z-index: 70（最高层级）
- **关闭按钮** - 右上角，红色悬停效果
- **切换按钮** - 左右两侧，悬停时显示

### 交互效果
- 照片卡片：悬停放大（scale 1.02）
- 切换按钮：默认隐藏，悬停显示
- 关闭按钮：始终显示

---

## 🧪 测试步骤

### 1. 清除浏览器缓存
```
Ctrl + Shift + R (Windows/Linux)
Cmd + Shift + R (Mac)
```

### 2. 访问网站
```
http://localhost
或
http://你的公网IP
```

### 3. 测试功能
- [ ] 点击照片卡片，弹出预览
- [ ] 点击背景，关闭预览
- [ ] 点击左箭头，显示上一张
- [ ] 点击右箭头，显示下一张
- [ ] 按键盘 ← → 切换照片
- [ ] 按 Esc 关闭预览

---

## 🐛 故障排查

### 问题 1：点击无反应
**可能原因：** 浏览器缓存旧代码

**解决方法：**
1. 清除缓存（Ctrl+Shift+R）
2. 打开开发者工具（F12）
3. 在 Network 标签勾选"Disable cache"
4. 刷新页面

### 问题 2：控制台报错
**检查方法：** F12 → Console

**常见错误：**
- `viewPhoto is not defined` → 缓存问题，强制刷新
- `currentPhotos is undefined` → 代码未正确加载

### 问题 3：照片不显示
**检查方法：** F12 → Network，查看图片请求

**可能原因：**
- 图片路径错误
- 服务器未正确返回图片

---

## 📊 代码结构

### 全局变量
```javascript
let currentPhotos = [];       // 当前分类的所有照片
let currentPhotoIndex = -1;   // 当前查看的照片索引
let currentCategory = '';     // 当前分类
```

### 核心函数
```javascript
viewPhoto(index)          // 查看指定索引的照片
showNextPhoto()           // 显示下一张
showPrevPhoto()           // 显示上一张
closePhotoModal()         // 关闭预览
handleModalBackdropClick() // 处理背景点击
```

### 事件监听
```javascript
// 照片卡片点击
card.onclick = () => viewPhoto(index)

// 键盘事件
document.addEventListener('keydown', ...)

// 背景点击
modal.onclick = handleModalBackdropClick
```

---

## 🎯 验证命令

```bash
# 检查代码是否部署
curl http://localhost/ | grep "showNextPhoto"
curl http://localhost/ | grep "currentPhotos"

# 检查服务状态
sudo systemctl status lensview

# 查看服务日志
sudo journalctl -u lensview -f
```

---

## 📝 更新日志

**2026-04-20 15:43**
- ✅ 添加照片大图预览功能
- ✅ 支持上一张/下一张切换
- ✅ 添加键盘快捷键支持
- ✅ 点击背景关闭预览
- ✅ 照片计数显示
- ✅ 智能预加载下一张

---

## 💡 提示

1. **首次使用** 一定要清除浏览器缓存
2. **测试功能** 建议先上传多张照片
3. **键盘操作** 需要先打开预览窗口
4. **移动端** 点击屏幕两侧切换照片（待实现）
